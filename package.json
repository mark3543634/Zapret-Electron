{
  "name": "zapret-pro",
  "version": "5.1.0",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "build": "electron-packager . ZapretPro --platform=win32 --arch=x64 --out=out --overwrite && node set-admin-manifest.js"
  },
  "devDependencies": {
    "electron": "^28.0.0",
    "electron-packager": "^17.1.2",
    "rcedit": "^3.1.0"
  }
}
